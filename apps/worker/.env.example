NODE_ENV=development
PORT=3321
API_PREFIX=

APP_DATABASE_URL=postgresql://riascout:riascout@localhost:5442/riascout?schema=app

MARKET_DATA_DIR=

INNGEST_DEV=1

# the worker sends queued mail (workspace invites). MAIL_TRANSPORT=log needs no
# key and prints instead of sending.
MAIL_TRANSPORT=resend
RESEND_API_KEY=
MAIL_FROM=
